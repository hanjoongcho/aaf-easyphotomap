<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.blog.korn123.easyphotomap" >

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <!--android:theme="@android:style/Theme.Translucent.NoTitleBar"-->
        <!--android:theme="@android:style/Theme.NoTitleBar.Fullscreen"-->
        <activity
            android:name=".helper.IntroActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen"
            android:label="@string/title_activity_maps" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MapsActivity" android:label="MapsActivity" />
        <activity android:name=".file.FileExplorerActivity" android:label="FileExplorer" android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".thumbnail.ThumbnailExplorerActivity" android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".search.PhotoSearchActivity" android:label="SearchFragment"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".search.AddressSearchActivity" android:label="SearchFragment"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".setting.EPMPreferenceActivity" android:label="EPMPreferenceActivity" />
        <activity android:name=".setting.SettingsActivity" android:label="SettingsActivity" />
        <activity android:name=".camera.CameraActivity" android:label="CameraActivity" android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".helper.PopupImageActivity" android:label="PopupImageActivity" android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".file.BatchPopupActivity" android:label="BatchPopupActivity" android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".timeline.TimelineActivity" android:label="Timeline"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".setting.LicensesActivity" android:label="Licenses" android:theme="@style/AppTheme.NoActionBar" />

    </application>

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />

</manifest>
